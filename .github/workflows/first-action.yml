name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello"
      - name: Multi-Line Shell Commands
        run: |
          echo "First output"
          echo "Second output"
      - name: Print goodbye
        run: echo "Bye"
